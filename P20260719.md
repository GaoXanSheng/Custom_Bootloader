# P20260719 — 基于 UEFI Bootloader 的 ACPI 运行时注入与功率墙解锁技术研究

## 摘要

本文提出一种基于 UEFI（Unified Extensible Firmware Interface）引导层拦截的 ACPI（Advanced Configuration and Power Interface）二级系统描述表（SSDT）注入与内存驻留补丁方案。针对蛟龙 16 PRO 笔记本电脑在固件级 ACPI 表中内置的全平台功率限制（Global Power Wall），本文设计并实现了一个轻量级引导加载器。该引导加载器在 Windows Boot Manager 加载之前执行，通过重建扩展系统描述表（XSDT）注入自定义 WMI（Windows Management Instrumentation）设备，同时采用一套数据驱动的模块化补丁引擎——以纯文本规格文件定义、Python 代码生成器自动转换为 C 编译时数据、运行时通过 FACP 间接定位 DSDT 实施跨表修补——对 OEM 厂商固件中的 7 个功率参数（ATPP/ATP2/TPPA/CSPL/FPPT）及 2 个逻辑条件（DBFS 钳位 / 65W 限制）进行内存级热替换。注：ACPI 中功耗寄存器采用 ×0.5 倍率编码，即寄存器值 `0x0168`（360）对应的实际物理功率为 360 × 0.5 = 180W。实验结果表明，该方案成功将平台功率限制从 180W 实际值（寄存器 `0x0168`）提升至 240W 实际值（寄存器 `0x01E0`），CS 慢速功耗限制从 45W 提升至 60W，快速功耗跟踪从 65W 提升至 80W，并通过 `root\wmi` 命名空间下的 `CustomDbUnlock` 类暴露了 NVIDIA Dynamic Boost 的运行时控制接口。全文以源码级精度记录了每个协议 GUID、每处二进制搜索模式以及每条 AML 操作码的完整语义，确保所述数据的可复现性与准确无误性。

**关键词：** UEFI Bootloader；ACPI SSDT 注入；XSDT 重建；WMI 设备；AML 字节码补丁；模块化补丁引擎；功率墙解锁；写时复制


## 1 引言

### 1.1 研究背景

现代 x86-64 计算平台中，ACPI 承担着操作系统与平台固件之间的关键抽象层角色。ACPI 表族中的 SSDT（Secondary System Description Table）通过 AML（ACPI Machine Language）字节码定义了平台设备的热管理策略、功耗上限和动态调频行为。在蛟龙 16 PRO（基于 AMD Ryzen 平台）固件中，OEM 厂商（INSYDE）通过 SSDT4 表（OEM Table ID: `EDK2`）在 `\_SB.NPCF` 设备（NVIDIA Platform Controller Framework，`_HID = "NVDA0820"`）中硬编码了多组热管理参数：ATPP（Total Processor Power）、ATP2（Adaptive Thermal Power 2）、TPPA（Total Platform Power Allowance）、CSPL（CPU Slow Package Limit）和 FPPT（Fast Package Power Tracking）。此外，DSDT 表（OEM Table ID: `EDK2`）的 FNQS 方法中通过 `LEqual(DBFS, One)` 条件分支在 Dynamic Boost 启用时施加了额外热管理约束。该平台的功耗寄存器采用 ×0.5 倍率换算（寄存器值 × 0.5 = 实际瓦数）。当系统检测到独立 GPU 且 CPU 类型为 Ryzen 7 时，ATPP 被强制重写为 `0x0168`（寄存器值 360，实际 180W），而厂商未触发该条件时的默认值为 `0x01B8`（寄存器值 440，实际 220W），形成了一道事实上的全平台功率墙——将平台总功耗上限压低了 40W（从 220W 降至 180W）。类似地，CSPL 被硬编码为 45W（Ryzen 7）、FPPT 被硬编码为 65W（Ryzen 7），均低于对应硬件的热设计能力。

传统修改 ACPI 表的方法包括通过 DSDT/SSDT 覆盖（`acpi_override`）、Clover/OpenCore 引导器注入或固件反向工程重刷。上述方法各有局限：`acpi_override` 依赖操作系统内核已加载后的 initrd 机制、第三方引导器带来复杂的配置文件管理负担、固件重刷存在不可逆的硬砖风险。本文提出一种折中方案：编写一个极小化的原生 UEFI 应用程序（无 EDK2/gnu-efi 依赖），在 Boot Manager 加载链路中充当透明代理：注入自定义 SSDT 后立即链式加载（chainload）`bootmgfw.efi`，对 Windows 内核完全透明。

### 1.2 主要贡献

1. 提出并实现了无需第三方 SDK 的裸 UEFI 引导加载器架构，手动声明所有 UEFI 协议表与 GUID。
2. 设计了一种 XSDT 物理页重建的"附加条目"注入算法，在保持原表不变的前提下追加自定义 SSDT 入口指针，并严格遵循 ACPI 规范同步更新 RSDP 两级校验和。
3. 设计了一套模块化补丁引擎：补丁以纯文本规格文件定义（`patches/*.txt`），通过 Python 脚本自动生成 C 头文件（`generate_patches.py` → `GeneratedPatches.h`），运行时以数据驱动方式遍历补丁表执行字节级替换。该架构将补丁逻辑与控制流完全解耦，新增补丁只需创建文本文件而无需修改 C 源码。
4. 实现了跨表补丁分发机制：不仅直接修补 XSDT 中的 SSDT 条目，还通过 FACP 表的 X_DsdtAddr/DsdtAddr 字段间接定位并修补 DSDT，覆盖了位于不同表族中的热管理代码路径。
5. 通过自定义 WMI MOF 类与 ASL 方法协同，在 `root\wmi` 命名空间中暴露了与 EC（Embedded Controller）交互的 DBFS 标志读写接口，为上层 PowerShell/WMIC 等工具提供标准化的 Dynamic Boost 运行时开关控制。

### 1.3 目标平台参数

| 参数 | 值 |
|---|---|
| 设备型号 | 蛟龙 16 PRO |
| 固件供应商 | INSYDE |
| ACPI 表总数（含注入前） | DSDT + 24 SSDT |
| 目标表 OEM Table ID | `EDK2`（ASCII: `"EDK2    "`，`0x20202020324B4445`）—— 同时匹配 DSDT 与 SSDT4 |
| 补丁数量 | 9 个（atpp_unlock, atp2_unlock, tppa_unlock, cspl_60w, cspl_r9_60w, fppt_80w, fppt_r9_80w, cpu_clamp_unlock, cpu_limit_65w） |
| 功率倍率 | ×0.5（寄存器值 × 0.5 = 实际瓦数） |
| 补丁引擎 | 模块化数据驱动：`patches/*.txt` → `generate_patches.py` → `GeneratedPatches.h` → `gPatches[]` |


## 2 系统架构与执行流程

### 2.1 总体架构

```
┌─────────────────────────────────────────────────┐
│                   UEFI 固件                       │
│  ┌─────────────────────────────────────────┐    │
│  │           Boot Manager                  │    │
│  │  加载 BOOTX64.efi（ESP\EFI\BOOT\）       │    │
│  └──────────────┬──────────────────────────┘    │
└─────────────────┼───────────────────────────────┘
                  ▼
┌─────────────────────────────────────────────────┐
│            efimain() 入口                        │
│  ┌─────────────────────────────────────────┐    │
│  │ 1. 通过 gEfiAcpi20TableGuid 定位 RSDP     │    │
│  │ 2. InjectSsdt() — 注入自定义 SSDT         │    │
│  │ 3. PatchSsdt4PowerWall() — 功率墙补丁     │    │
│  │ 4. 构建 Device Path → LoadImage()         │    │
│  │ 5. StartImage() → 链式加载 bootmgfw.efi  │    │
│  └─────────────────────────────────────────┘    │
└──────────────────┬──────────────────────────────┘
                   ▼
┌─────────────────────────────────────────────────┐
│           Windows Boot Manager                   │
│      (bootmgfw.efi) — 对注入的 SSDT 完全透明      │
└──────────────────┬──────────────────────────────┘
                   ▼
┌─────────────────────────────────────────────────┐
│              Windows 内核                         │
│  • 解析 XSDT 中的所有 SSDT（含注入的 DBUL 设备）   │
│  • 注册 WMI 提供程序 CustomDbUnlock               │
│  • 应用 ATPP/ATP2 已修补的功率参数                │
└─────────────────────────────────────────────────┘
```

### 2.2 模块划分

| 源文件 | 行数 | 功能 |
|---|---|---|
| `src/main.c` | 125 | `efi_main` 入口，编排注入、补丁、链式加载全流程 |
| `src/UefiHelpers.h` | 235 | 手动声明的 UEFI 类型系统、协议 GUID、ACPI 结构体 |
| `src/UefiHelpers.c` | 135 | `memcpy`、校验和、设备路径拼接等基础设施 |
| `src/Logging.c` | 105 | ESP 文件日志子系统（`\EFI\BOOT\unlock.log`） |
| `src/AcpiPatch.c` | 385 | XSDT/FACP 重建、SSDT/DSDT 影子页分配、补丁遍历执行 |
| `src/GeneratedPatches.h` | 85 | 自动生成的补丁数据（`gPatches[]` 数组，由 `generate_patches.py` 生成） |
| `src/acpi/SsdtUnlockDB.asl` | 110 | 自定义 WMI 设备 ASL 定义 |
| `src/acpi/SsdtUnlockDB.mof` | 20 | WMI MOF 类契约 |
| `tools/generate_patches.py` | ~80 | 补丁代码生成器：扫描 `patches/*.txt` → 生成 `GeneratedPatches.h` |
| `build.bat` | 60 | 完整编译流水线：MOF→BMF→ASL→AML→补丁代码生成→C→EFI |

### 2.3 执行流程图（按函数调用顺序）

```
efi_main()
 │
 ├─[1] 遍历 ConfigurationTable[]
 │      └─ CompareGuid(→gEfiAcpi20TableGuid)
 │         └─ 找到 → 提取 RSDP（EFI_ACPI_2_0_ROOT_SYSTEM_DESCRIPTION_POINTER）
 │
 ├─[2] InjectSsdt(SystemTable, ImageHandle, Rsdp)
 │      ├─ AllocatePages(AllocateType=0, MemoryType=9, Pages)
 │      │   └─ 将 SsdtUnlockDB.aml 逐字节拷贝至 ACPI NVS 内存
 │      ├─ 计算 NewXsdtLength = OriginalXsdtLength + 8
 │      ├─ AllocatePages(AllocateType=0, MemoryType=9, Pages)
 │      │   └─ 完整拷贝原 XSDT → 末尾追加 (UINT64)InjectTableBuffer
 │      ├─ 重算 NewXsdt->Checksum
 │      ├─ 更新 Rsdp->XsdtAddress → 指向新 XSDT
 │      ├─ 重算 Rsdp->Checksum（前 20 字节校验和）
 │      └─ 重算 Rsdp->ExtendedChecksum（全 Rsdp->Length 校验和，至少 36 字节）
 │
 ├─[3] PatchSsdt4PowerWall(SystemTable, ImageHandle, Rsdp)
 │      ├─ 遍历 XSDT 所有 EntryPtr[0..EntryCount-1]
 │      │   ├─ IsValidAcpiPointer() 校验地址：非空、4 字节对齐、
 │      │   │   地址 ∈ [0x100000, 0x4000000000]
 │      │   │
 │      │   ├─ [SSDT 分支] Signature == 'SSDT' (0x54445353)
 │      │   │   └─ 遍历 gPatches[]，若 Table->OemTableId 匹配:
 │      │   │       分配影子页 → 拷贝完整 SSDT → SearchAndReplace()
 │      │   │       ├─ atpp_unlock:      70 0B 68 01 ... ATPP → 70 0B E0 01 ... ATPP
 │      │   │       ├─ atp2_unlock:      70 0B 68 01 ... ATP2 → 70 0B E0 01 ... ATP2
 │      │   │       ├─ tppa_unlock:      70 0B 68 01 ... TPPA → 70 0B E0 01 ... TPPA
 │      │   │       ├─ cspl_60w:        70 0A 2D 60  → 70 0A 3C 60
 │      │   │       ├─ cspl_r9_60w:     70 0A 37 60  → 70 0A 3C 60
 │      │   │       ├─ fppt_80w:        70 0A 41 60  → 70 0A 50 60
 │      │   │       ├─ fppt_r9_80w:     70 0A 4B 60  → 70 0A 50 60
 │      │   │       ├─ cpu_clamp_unlock: 93 44 42 46 53 01 → 93 44 42 46 53 FF
 │      │   │       └─ cpu_limit_65w:    0B C8 AF → 0B FF FF
 │      │   │   └─ 补丁成功 → 重算 NewTable->Checksum → EntryPtr[Index] = NewTableAddr
 │      │   │
 │      │   └─ [FACP 分支] Signature == 'FACP' (0x50434146)
 │      │       ├─ 读取 X_DsdtAddr（偏移 140）/ DsdtAddr（偏移 40）
 │      │       ├─ 若 DSDT 有效 → 分配 DSDT 影子页 → 遍历 gPatches[]
 │      │       │   └─ OemTableId 匹配 → SearchAndReplace()（同上述 9 个补丁）
 │      │       ├─ 补丁成功 → 重算 DSDT Checksum → 更新 FACP 的 DSDT 指针
 │      │       ├─ 重算 FACP Checksum → EntryPtr[Index] = NewFacpAddr
 │      │       └─ 补丁未匹配 → 释放影子页，保持原指针
 │      │
 │      └─ 若有任何补丁成功:
 │          ├─ 重算 Xsdt->Checksum
 │          └─ 返回 EFI_SUCCESS
 │
 └─[4] 链式加载 Windows Boot Manager
        ├─ LoadImage() for Candidates[0]:  "EFI\Microsoft\Boot\bootmgfw.efi"
        │   └─ 失败 → 继续
        ├─ LoadImage() for Candidates[1]:  "\EFI\Microsoft\Boot\bootmgfw.efi"
        ├─ 一旦成功 → StartImage(WinBootHandle)
        └─ 全部失败 → Stall(10000000μs) → 返回 EFI_NOT_FOUND
```


## 3 关键技术实现细节

### 3.1 零依赖 UEFI 类型系统

本引导加载器不链接 EDK2（TianoCore）或 gnu-efi 的任何库。`UefiHelpers.h` 中以 `#pragma pack(push, 1)` 精确声明了所有必需的 UEFI 协议结构体。核心类型映射如表 1：

**表 1 — 手动声明的 UEFI 基本类型**

| 项目自定义类型 | 实际 C 类型 | UEFI 规范对应 |
|---|---|---|
| `UINT8` | `unsigned char` | `UINT8` |
| `UINT16` | `unsigned short` | `UINT16` |
| `UINT32` | `unsigned int` | `UINT32` |
| `UINT64` | `unsigned long long` | `UINT64` |
| `UINTN` | `size_t` | `UINTN` |
| `BOOLEAN` | `short` | `BOOLEAN` |
| `EFI_STATUS` | `UINTN` | `EFI_STATUS` |
| `EFI_PHYSICAL_ADDRESS` | `UINT64` | `EFI_PHYSICAL_ADDRESS` |
| `EFI_HANDLE` | `void *` | `EFI_HANDLE` |

调用约定通过 `#define EFIAPI __cdecl` 统一为 Microsoft x64 调用约定。错误判定宏 `EFI_ERROR(status)` 定义为 `(((long long)(status)) < 0)`，与 UEFI 规范的最高位错误编码完全一致。

### 3.2 ACPI RSDP 定位机制

`efi_main` 通过 `SystemTable->ConfigurationTable[]` 逐项扫描，执行 `CompareGuid()` 匹配 ACPI 2.0 表 GUID：

**ACPI 2.0 协议 GUID 精确值：**
```
Data1: 0x8868E871
Data2: 0xE4F1
Data3: 0x11D3
Data4: { 0xBC, 0x22, 0x00, 0x80, 0xC7, 0x3C, 0x88, 0x81 }
```

对比算法采用 128 位分解：先比较 `Data1`、`Data2`、`Data3`，再逐字节比较 `Data4[0..7]`。该算法是 O(1) 提前退出的优化版本——任一字段不等即返回 `FALSE`。

### 3.3 RSDP 结构体定义（精确内存布局）

```c
#pragma pack(push, 1)
typedef struct {
    UINT64 Signature;           // Offset 0:  "RSD PTR "
    UINT8  Checksum;            // Offset 8:  20 字节校验和
    UINT8  OemId[6];            // Offset 9:  OEM ID
    UINT8  Revision;            // Offset 15: 必须为 2（ACPI 2.0+）
    UINT32 RsdtAddress;         // Offset 16: 32 位 RSDT 指针
    UINT32 Length;              // Offset 20: RSDP 结构长度（≥36）
    UINT64 XsdtAddress;         // Offset 24: 64 位 XSDT 指针 —— 注入核心修改点
    UINT8  ExtendedChecksum;    // Offset 32: 全结构校验和
    UINT8  Reserved[3];         // Offset 33
} EFI_ACPI_2_0_ROOT_SYSTEM_DESCRIPTION_POINTER;
#pragma pack(pop)
// sizeof = 36 字节
```

**表 2 — ACPI SDT 通用头（EFI_ACPI_SDT_HEADER）**

| 偏移 | 字段 | 大小 | 说明 |
|---|---|---|---|
| 0 | `Signature` | 4 字节 | e.g. `SSDT` = `0x54445353`（小端：'S' 'S' 'D' 'T'） |
| 4 | `Length` | 4 字节 | 表总长度（含头） |
| 8 | `Revision` | 1 字节 | 修订号 |
| 9 | `Checksum` | 1 字节 | 8 位模 256 校验和（补零后求和为 0） |
| 10 | `OemId`[6] | 6 字节 | 本平台为 `"INSYDE"` |
| 16 | `OemTableId` | 8 字节 | 本平台为 `"EDK2   "`（`0x20202020324B4445`） |
| 24 | `OemRevision` | 4 字节 | OEM 修订号 |
| 28 | `CreatorId` | 4 字节 | 创建者 ID |
| 32 | `CreatorRevision` | 4 字节 | 创建者修订号 |

**sizeof(EFI_ACPI_SDT_HEADER) = 36 字节。**

### 3.4 自定义 SSDT 注入算法（InjectSsdt）

#### 3.4.1 内存分配策略

| 参数 | 值 | 语义 |
|---|---|---|
| `AllocateType` | `0` | `AllocateAnyPages` — 由固件任意分配物理页 |
| `MemoryType` | `9` | `EfiACPIReclaimMemory` — ACPI 可回收内存，系统在引导加载后可回收利用 |
| 分配粒度 | 4KB（EFI_PAGE_SIZE） | 标准 UEFI 页大小 |

页数计算公式：`PagesNeeded = (sizeof(ssdtunlockdb_aml_code) + 4095) / 4096`（向上取整除以页大小）。

#### 3.4.2 XSDT 重建步骤

1. **获取原 XSDT 长度**：`OriginalXsdtLength = Xsdt->Length`
2. **计算新 XSDT 长度**：`NewXsdtLength = OriginalXsdtLength + sizeof(UINT64)`，即追加 8 字节（一个 64 位指针）
3. **分配新 XSDT 内存页**：`AllocatePages(0, 9, XsdtPagesNeeded, &NewXsdtAddr)`
4. **完整拷贝**：`UefiMemcpy(NewXsdt, Xsdt, OriginalXsdtLength)`
5. **写入新条目**：`((UINT64 *)((UINT8 *)NewXsdt + OriginalXsdtLength))[0] = (UINT64)InjectTableBuffer`
6. **更新 XSDT 头**：`NewXsdt->Length = (UINT32)NewXsdtLength`，清零 `Checksum` 后重新计算
7. **更新 RSDP 根指针**：`Rsdp->XsdtAddress = (UINT64)NewXsdtAddr`

#### 3.4.3 三阶段校验和重算

校验和算法 `CalculateChecksum8()` 定义如下：

```c
UINT8 CalculateChecksum8(UINT8 *Buffer, UINTN Size) {
    UINT8 Sum = 0;
    for (UINTN i = 0; i < Size; i++) Sum += Buffer[i];
    return (UINT8)(0 - Sum);  // 补码，使全表字节和为零
}
```

修改 RSDP 的 `XsdtAddress` 字段后，必须执行三阶段校验和同步：

| 阶段 | 校验对象 | 范围 | 字段 |
|---|---|---|---|
| 1 | 新 XSDT | `NewXsdt->Length` 字节 | `NewXsdt->Checksum` |
| 2 | RSDP 旧格式 | 前 20 字节 | `Rsdp->Checksum`（ACPI 1.0 兼容） |
| 3 | RSDP 新格式 | `Rsdp->Length` 字节（至少 36） | `Rsdp->ExtendedChecksum`（ACPI 2.0+） |

**关键边界保护：** 在重算 `ExtendedChecksum` 前，代码强制 `RsdpLength = Rsdp->Length; if (RsdpLength < 36) RsdpLength = 36;`，以防止异常短 RSDP 导致的越界访问。

#### 3.4.4 自定义 SSDT 的 ASL 规范

自定义 SSDT 的 AML 编译自 `SsdtUnlockDB.asl`：

```asl
DefinitionBlock ("", "SSDT", 2, "CUSTOM", "DBUNLOCK", 0x00001000)
{
    External (\DBFS, IntObj)
    External (\_SB.PCI0.LPC0, DeviceObj)
    External (\_SB.PCI0.LPC0.H_EC.COMM, MethodObj)

    Scope (\_SB.PCI0.LPC0) {
        Device (DBUL) {
            Name (_HID, EisaId ("PNP0C14"))           // WMI 设备标准 HID
            Name (_UID, 0xAA)                          // 设备唯一标识符
            Name (_STA, 0x0F)                          // 设备存在 + 启用 + 可用
            ...
        }
    }
}
```

**表 3 — DBUL 设备关键属性**

| ACPI 属性 | 值 | 含义 |
|---|---|---|
| `_HID` | `PNP0C14` | 即插即用 ID，Windows WMI 映射标识 |
| `_UID` | `0xAA` | 设备实例唯一 ID |
| `_STA` | `0x0F` | 设备存在(bit0) + 启用(bit1) + 显示(bit2) + 功能正常(bit3) |
| 设备路径 | `\_SB.PCI0.LPC0.DBUL` | 挂在 PCI0 总线 LPC0 桥下 |
| SSDT 版本 | 2 | ACPI 表版本 |
| OEM ID | `"CUSTOM"` | 自定义 OEM 标识 |
| OEM Table ID | `"DBUNLOCK"` | 自定义表标识 |
| OEM Revision | `0x00001000` | 自定义修订号 |

### 3.5 模块化补丁引擎：数据驱动的多目标二进制补丁系统

#### 3.5.1 架构演进：从硬编码到数据驱动

在项目的早期版本中，功率墙补丁逻辑直接硬编码在 `AcpiPatch.c` 中——ATPP 和 ATP2 的搜索模式与替换模式以内联字节数组形式嵌入函数体。每新增一个补丁目标都需要修改 C 源码、重新编译整个 EFI 映像。这带来了两个核心问题：(1) 控制流与补丁数据紧密耦合，补丁逻辑的可维护性随数量增长急剧下降；(2) 逆向分析人员（使用 iASL 反编译 AML、定位偏移量）与 C 开发者之间存在非必要的手工转换环节。

为解决上述问题，本文设计了一套完全数据驱动的模块化补丁引擎，其架构如下：

```
patches/*.txt  →  generate_patches.py  →  src/GeneratedPatches.h  →  gPatches[]  →  SearchAndReplace()
   (规格文件)      (Python 代码生成器)       (自动生成的头文件)         (补丁表数组)     (运行时引擎)
```

补丁规格文件（`.txt`）以 `table=<OEM Table ID>` / `find=<hex>` / `replace=<hex>` 的简洁格式定义补丁数据。Python 脚本 `generate_patches.py` 扫描 `patches/` 目录下的所有 `.txt` 文件，生成包含 `ACPI_PATCH` 结构体数组的 C 头文件 `GeneratedPatches.h`。运行时 `AcpiPatch.c` 仅需遍历 `gPatches[]` 数组，对匹配的 ACPI 表调用 `SearchAndReplace()` 即可——新增补丁只需创建文本文件后重新构建，无需触碰任何 C 源码。

#### 3.5.2 补丁规格文件格式与代码生成

每个补丁文件位于 `patches/<name>.txt`，文件名即为补丁名称（用于日志输出）。文件内容格式示例（`patches/atpp_unlock.txt`）：

```
table=EDK2
find=700B680141545050
replace=700BE00141545050
```

**字段语义：**

| 字段 | 说明 | 示例 |
|---|---|---|
| `table` | 目标 ACPI 表的 OEM Table ID 字符串（8 字符，不足尾部补空格） | `EDK2` → `0x20202020324B4445ULL` |
| `find` | 十六进制编码的搜索字节序列（无空格） | `700B680141545050`（8 字节） |
| `replace` | 十六进制编码的替换字节序列（必须与 `find` 等长） | `700BE00141545050`（8 字节） |

代码生成器 `generate_patches.py` 为每个补丁文件生成三组静态数组：

```c
// 搜索/替换字节数组
static const UINT8 Patch_atpp_unlock_Find[]    = { 0x70, 0x0B, 0x68, 0x01, 0x41, 0x54, 0x50, 0x50 };
static const UINT8 Patch_atpp_unlock_Replace[] = { 0x70, 0x0B, 0xE0, 0x01, 0x41, 0x54, 0x50, 0x50 };

// 日志用宽字符名称
static const CHAR16 Patch_atpp_unlock_WName[] = L"atpp_unlock";
```

最终聚合为全局补丁表：

```c
static const ACPI_PATCH gPatches[] = {
    { 0x20202020324B4445ULL, Patch_atpp_unlock_Find, 8, Patch_atpp_unlock_Replace, 8, Patch_atpp_unlock_WName },
    { 0x20202020324B4445ULL, Patch_atp2_unlock_Find, 8, Patch_atp2_unlock_Replace, 8, Patch_atp2_unlock_WName },
    // ... 其余 7 个补丁
};
static const UINTN gPatchCount = 9;
```

`ACPI_PATCH` 结构体定义如下：

```c
typedef struct {
    UINT64 OemTableId;      // 目标表 OEM Table ID（64 位 ASCII 编码）
    const UINT8 *Search;    // 搜索模式起始地址
    UINTN SearchLen;        // 搜索模式长度（字节）
    const UINT8 *Replace;   // 替换模式起始地址
    UINTN ReplaceLen;       // 替换模式长度（字节）
    const CHAR16 *Name;     // 补丁名称（宽字符，日志输出用）
} ACPI_PATCH;
```

所有 9 个补丁均以 `0x20202020324B4445ULL`（即 ASCII `"EDK2    "`）为 `OemTableId`，匹配 DSDT 和 SSDT4 两张表。运行时的表匹配条件为 `Table->OemTableId == Patch->OemTableId`，采用 64 位整数等值比较。

#### 3.5.3 补丁清单与 AML 字节码规格

**表 4 — 全部 9 个补丁的 AML 字节码规格**

| # | 补丁名称 | 搜索长度 | 搜索模式（hex） | 替换模式（hex） | 目标变量 | 语义 |
|---|---------|---------|---------------|---------------|---------|------|
| 1 | `atpp_unlock` | 8 | `70 0B 68 01 ... ATPP` | `70 0B E0 01 ... ATPP` | ATPP | 功率上限 180W→240W |
| 2 | `atp2_unlock` | 8 | `70 0B 68 01 ... ATP2` | `70 0B E0 01 ... ATP2` | ATP2 | 功率上限 180W→240W |
| 3 | `tppa_unlock` | 8 | `70 0B 68 01 ... TPPA` | `70 0B E0 01 ... TPPA` | TPPA | 功率上限 180W→240W |
| 4 | `cspl_60w` | 4 | `70 0A 2D 60` | `70 0A 3C 60` | CSPL | 慢速功耗限制 45W→60W |
| 5 | `cspl_r9_60w` | 4 | `70 0A 37 60` | `70 0A 3C 60` | CSPL | R9 慢速功耗限制 55W→60W |
| 6 | `fppt_80w` | 4 | `70 0A 41 60` | `70 0A 50 60` | FPPT | 快速功耗限制 65W→80W |
| 7 | `fppt_r9_80w` | 4 | `70 0A 4B 60` | `70 0A 50 60` | FPPT | R9 快速功耗限制 75W→80W |
| 8 | `cpu_clamp_unlock` | 6 | `93 44 42 46 53 01` | `93 44 42 46 53 FF` | DBFS | 解除 DBFS=1 热管理钳位 |
| 9 | `cpu_limit_65w` | 3 | `0B C8 AF` | `0B FF FF` | (未知) | 解除 65W CPU 限制 |

##### 补丁 #1–#3：功率墙解锁（ATPP/ATP2/TPPA）

这三个补丁的结构高度相似——均以 `0x70`（`Store` 操作码）开始，后跟 `0x0B`（NameString 前缀）、目标值（小端序 `0x0168` = 寄存器值 360，实际 180W），以及 4 字节 ASCII 变量名。以 `atpp_unlock` 为例：

```
搜索: 0x70, 0x0B, 0x68, 0x01, 0x41, 0x54, 0x50, 0x50
       │     │     └──┬──┘  └────────┬────────┘
       │     │  0x0168 (360→180W)   "ATPP" (ASCII)
       │     └─ NameString 前缀
       └─ Store 操作码

替换: 0x70, 0x0B, 0xE0, 0x01, 0x41, 0x54, 0x50, 0x50
                   │
          0x01E0 (480→240W, +60W)
```

`atp2_unlock` 与 `tppa_unlock` 的唯一区别在于末尾的 NameString：`"ATP2"`（`0x41 0x54 0x50 0x32`）和 `"TPPA"`（`0x54 0x50 0x50 0x41`）。三者均将目标值从 `0x0168` 替换为 `0x01E0`，实现 +60W（+33.3%）的功率增益。

**SSDT4 中的匹配位置：** `atpp_unlock` 在 SSDT4 的 AML 字节码中匹配 **3 处**（各自对应 DGPU + CPUT=0x07、ITSM 各分支条件），`atp2_unlock` 匹配 **1 处**，`tppa_unlock` 匹配 **1 处**。

##### 补丁 #4–#7：CSPL/FPPT 提升

CSPL（CPU Slow Package Limit）和 FPPT（Fast Package Power Tracking）是 AMD SMU（System Management Unit）功率管理参数。OEM 固件在 SSDT4 中为 Ryzen 7 和 Ryzen 9 分别硬编码了保守的限制值：

| 参数 | OEM Ryzen 7 | OEM Ryzen 9 | 补丁目标 | 增益 |
|---|---|---|---|---|
| CSPL | 45W（`0x2D` = 45 × 1.0） | 55W（`0x37`） | 60W（`0x3C`） | +15W / +5W |
| FPPT | 65W（`0x41`） | 75W（`0x4B`） | 80W（`0x50`） | +15W / +5W |

字节码模式均为 4 字节：`0x70 0x0A <value> 0x60`，其中 `0x60` 是 AML 中该操作数的高字节标志。注意 AMI 平台对该寄存器的编码使用 ×1.0 倍率（非 ×0.5）。

##### 补丁 #8：cpu_clamp_unlock — DBFS 条件钳位解除

此补丁是唯一不涉及功率数值变更的补丁——它修改一个布尔比较操作数：

```
搜索: 0x93, 0x44, 0x42, 0x46, 0x53, 0x01
       │     └───────┬───────┘    │
       │        "DBFS" (ASCII)    One (常量)
       └─ LEqual 操作码

替换: 0x93, 0x44, 0x42, 0x46, 0x53, 0xFF
                                     │
                              ByteConst 0xFF（永假）
```

原始 ASL 语义为 `If ((DBFS == One)) { ... }`——当 Dynamic Boost 启用时，执行额外的限制性热管理 profile。替换后的语义为 `If ((DBFS == 0xFF)) { ... }`——由于 `\DBFS` 的实际取值范围为 {0, 1}，该条件恒假，对应的限制性代码路径永不执行。

**匹配位置：** 该模式在 DSDT 的 FNQS 方法中匹配 **2 处**（分别在 Arg0=0 和 Arg0=1 分支内），均为 DGPU 存在 + 特定 CPUT 条件下的 DBFS 评估。由于 DSDT 不在 XSDT 的 SSDT 列表中，必须通过 §3.5.6 节所述的 FACP→DSDT 重定向机制进行修补。

##### 补丁 #9：cpu_limit_65w — 65W 限制解除

```
搜索: 0x0B, 0xC8, 0xAF
       │     └──┬──┘
       │  WordConst 0xAFC8（小端）= 45,000
       └─ WordPrefix

替换: 0x0B, 0xFF, 0xFF
             └──┬──┘
        WordConst 0xFFFF = 65,535（最大值）
```

将一个 16 位立即数从 45,000 替换为最大值 65,535，解除 OEM 的 65W CPU 功耗限制。该补丁仅 3 字节，是全部补丁中最短的模式。

#### 3.5.4 SearchAndReplace 算法分析

```c
static UINTN SearchAndReplace(
    UINT8 *Buffer, UINTN Size,
    const UINT8 *Search, UINTN SearchSize,
    const UINT8 *Replace, UINTN ReplaceSize)
```

**前置条件：** `SearchSize == ReplaceSize`（必须相等，否则直接返回 0）。

**搜索策略：** 滑动窗口，从 `Buffer[0]` 到 `Buffer[Size - SearchSize]`，逐字节比较。匹配成功后将 `SearchSize` 字节替换为 `Replace` 对应字节，计数器加 1。

**跳步策略：** 匹配后 `i += SearchSize - 1`，防止重叠匹配。

**时间复杂度：** O(Size × SearchSize)，最坏情况约为 9 个补丁 × 表长（SSDT4: 9306 字节 + DSDT: ~300KB）× 平均模式长度 6 字节 ≈ 约 18M 次字节比较，在 UEFI 引导阶段可忽略不计。

#### 3.5.5 写时复制（Copy-on-Write）内存方案

由于 OEM 固件的 ACPI 表可能存储在只读内存区域（UEFI 引导服务阶段的固件保留内存），本方案采用**不修改原表**的写时复制策略：

1. `AllocatePages(0, 9, PagesNeeded, &NewTableAddr)` —— 分配全新的 ACPI Reclaim 内存页
2. `UefiMemcpy(NewTable, Table, TableLength)` —— 完整复制 ACPI 表到影子页
3. 遍历 `gPatches[]`，对 `OemTableId` 匹配的补丁调用 `SearchAndReplace()` —— 在**影子页**上执行所有二进制替换
4. `NewTable->Checksum = 0; NewTable->Checksum = CalculateChecksum8(...)` —— 重算影子表校验和
5. `EntryPtr[Index] = (UINT64)NewTableAddr` —— 将 XSDT 指针重定向至影子表
6. `Xsdt->Checksum = 0; Xsdt->Checksum = CalculateChecksum8(...)` —— 更新 XSDT 自身校验和

若表的 `OemTableId` 与任何补丁均不匹配，则跳过该表。若补丁执行后未匹配任何模式（`PatchesApplied == 0`），则释放影子页内存并保持原始指针不变。

#### 3.5.6 FACP→DSDT 间接重定向机制

部分补丁（尤其是 `cpu_clamp_unlock`）的目标代码位于 DSDT 表中。DSDT 不在 XSDT 中以独立条目出现——其物理地址由 FACP（Fixed ACPI Description Table）的 `DsdtAddr`（偏移 40，32 位）和 `X_DsdtAddr`（偏移 140，64 位）字段间接引用。因此本引擎在遍历 XSDT 时额外处理 `Signature == 'FACP'`（`0x50434146`）的表：

```
FACP → 读取 X_DsdtAddr（偏移 140）
    │  （若为 0，回退至 DsdtAddr（偏移 40））
    │
    ├─ 验证 DSDT 指针：IsValidAcpiPointer() + Signature == 'DSDT' (0x54445344)
    │
    ├─ 分配 DSDT 影子页 → UefiMemcpy → 遍历 gPatches[] → SearchAndReplace()
    │
    ├─ 补丁成功 → 重算 DSDT Checksum
    │           → 更新 FACP 的 X_DsdtAddr 指向新 DSDT
    │           → 若原地址 ≤ 4GB，同步更新 DsdtAddr
    │           → 重算 FACP Checksum
    │           → EntryPtr[Index] = NewFacpAddr（重定向 XSDT 中 FACP 条目）
    │
    └─ 补丁未匹配 → 释放 DSDT 影子页 + FACP 影子页，保持原始指针
```

此三级重定向（XSDT→FACP→DSDT）是 DSDT 修补的必要前提——仅修改 SSDT 无法覆盖位于 DSDT 中的 FNQS 方法和 DBFS 条件分支。

#### 3.5.7 有效性校验

`IsValidAcpiPointer()` 对每个 XSDT 条目执行三重验证：

```c
if (Ptr == NULL)       return FALSE;  // 空指针拒绝
if ((Addr & 3) != 0)   return FALSE;  // 4 字节对齐检查
if (Addr < 0x100000ULL || Addr > 0x4000000000ULL) return FALSE;  // 地址范围检查
```

地址合法范围：1 MB (`0x100000`) 到 256 GB (`0x4000000000`)，覆盖了典型 x86-64 平台所有可能的 ACPI 表物理地址。

#### 3.5.8 OEM 表匹配逻辑与跨表分发

XSDT 条目数计算公式：
```
EntryCount = (Xsdt->Length - sizeof(EFI_ACPI_SDT_HEADER)) / sizeof(UINT64)
```

运行时遍历逻辑分为三个层级：

| 层级 | 条件 | 动作 |
|---|---|---|
| 1 — 直接 SSDT | `Signature == 'SSDT'` `&&` `OemTableId == 0x20202020324B4445` | 分配影子页 → 遍历全部 9 个补丁 |
| 2 — FACP→DSDT | `Signature == 'FACP'` | 定位 DSDT → 若 `OemTableId` 匹配 → 分配影子页 → 遍历全部 9 个补丁 |
| 3 — 其他 | 其余所有表 | 跳过（仅记录日志） |

关键设计决策：`OemTableId` 的比较在**表级别**而非补丁级别——若某表的 `OemTableId` 与 `gPatches[0].OemTableId` 匹配，则对该表执行全部 9 个补丁（每个补丁的 `SearchAndReplace` 独立运行，不匹配的模式自动跳过）。这简化了控制流，9 次滑动窗口搜索的额外开销在引导阶段可忽略不计。

### 3.6 WMI 接口设计

#### 3.6.1 MOF 类定义

MOF（Managed Object Format）定义了 WMI 命名空间中的类契约：

```c
class CustomDbUnlock {
    [key, read] string InstanceName;
    [read] boolean Active;

    [WmiMethodId(1), Implemented, Description("Set DBFS Flag")]
    void SetDbfs([in] uint32 Value, [out] uint32 Status);

    [WmiMethodId(2), Implemented, Description("Read DBFS Flag")]
    void ReadDbfs([out] uint32 Value);
};
```

**WMI GUID:** `{850C1B62-A3D9-4E65-B8D5-DE3C74C9B38D}`

该 GUID 同时出现在 MOF 类和 ASL 的 WDG（WMI Data Block）条目中，形成"MOF ↔ ACPI" 的双向绑定。

#### 3.6.2 WDG 缓冲区精确布局

WDG（WMI Data Block）缓冲区大小为 `0x28`（40 字节），包含两个条目：

```
Offset 0x00:  GUID (16 字节) → {850C1B62-A3D9-4E65-B8D5-DE3C74C9B38D}
Offset 0x10:  Object ID "TF" (2 字节 ASCII)
Offset 0x12:  Flags 0x01
Offset 0x13:  WMI Type 0x02 (WMI Method)
Offset 0x14:  GUID (16 字节) → {05901221-D566-11D1-B2F0-00A0C9062910}
Offset 0x24:  Object ID "BA" (2 字节 ASCII)
Offset 0x26:  Flags 0x01
Offset 0x27:  WMI Type 0x00 (Binary MOF Data)
```

**表 5 — WDG 条目详细说明**

| 条目 | Object ID | GUID | WMI 类型 | 说明 |
|---|---|---|---|---|
| WMTF | `TF`（`0x4654`） | `{850C1B62-A3D9-4E65-B8D5-DE3C74C9B38D}` | `0x02`（方法） | WMI 方法入口 |
| WQBA | `BA`（`0x4142`） | `{05901221-D566-11D1-B2F0-00A0C9062910}` | `0x00`（数据） | 二进制 MOF 配置数据 |

#### 3.6.3 WMTF 方法语义

WMTF 方法（Serialized，3 参数）实现了两个子命令：

**子命令 1 (SetDbfs)：** `Arg1 == 1`
1. 检查 `SizeOf(Arg2) >= 1`（输入缓冲区至少 1 字节）
2. `Local0 = DerefOf(Index(Arg2, 0))`（取 Arg2 首字节）
3. `Store(Local0, \DBFS)`（写入 DBFS 全局变量）
4. 调用 `\_SB.PCI0.LPC0.H_EC.COMM()`（通知 EC 设置变更）
5. `RVAL = 0`，返回 4 字节 Buffer

**子命令 2 (ReadDbfs)：** `Arg1 == 2`
1. 若 `\DBFS` 存在：`RVAL = \DBFS`
2. 若 `\DBFS` 不存在：`RVAL = 0xFFFFFFFF`
3. 返回 4 字节 Buffer

**返回值编码：** 所有返回值通过 `RBUF`（Buffer(4){}）编码，内部创建 `DWordField(RBUF, 0, RVAL)` 作为 32 位返回载体，与 MOF 定义的 `[out] uint32` 参数精确映射。

#### 3.6.4 _INI 初始化行为

设备加载时，`_INI` 方法自动执行：
1. `Store(Zero, \DBFS)` —— 默认禁用 Dynamic Boost
2. `\_SB.PCI0.LPC0.H_EC.COMM()` —— 立即提交 EC 设置

这确保了每次引导都从已知安全状态启动。

#### 3.6.5 用户态调用接口

通过 PowerShell 调用（Windows 平台）：

```powershell
# 获取 WMI 实例
$ins = Get-WmiObject -Namespace root\wmi -Class CustomDbUnlock -Authentication PacketPrivacy

# 禁用 Dynamic Boost
$result = $ins.SetDbfs(0)
Write-Host "Status: $($result.Status)"

# 启用 Dynamic Boost
$result = $ins.SetDbfs(1)
Write-Host "Status: $($result.Status)"

# 读取当前状态
$result = $ins.ReadDbfs()
Write-Host "DBFS Value: $($result.Value)"
```

### 3.7 链式加载与设备路径构建

#### 3.7.1 启动路径候选项

引导加载器尝试以下路径加载 `bootmgfw.efi`：

| 序号 | 候选路径 | 特点 |
|---|---|---|
| 0 | `"EFI\Microsoft\Boot\bootmgfw.efi"` | 相对路径（相对于 ESP 根） |
| 1 | `"\EFI\Microsoft\Boot\bootmgfw.efi"` | 绝对路径（从根目录起） |

**已知代码瑕疵：** `NumCandidates` 被硬编码为 `4`，但 `Candidates[]` 数组仅定义了 2 个字符串。循环从 `i=0` 迭代到 `i=3`，`Candidates[2]` 和 `Candidates[3]` 将读取未定义内存（栈上的随机值），构成潜在的未定义行为。在实际运行中，由于前两次迭代通常已成功加载，该瑕疵未引发可观测故障，但在形式验证层面属于正确性缺陷。

#### 3.7.2 设备路径拼接算法

`AppendFileNameToDevicePath()` 实现 ESP 分区路径与文件路径的拼接：

1. 计算分区设备路径大小（含结束节点）：`PartPathSize = GetDevicePathSize(PartPath)`
2. 计算不含结束节点的部分：`PartPathSizeNoEnd = PartPathSize - 4`（结束节点为 4 字节：Type=0x7F, SubType=0xFF, Length=4）
3. 构造文件路径节点：`Type=4`（Media Device Path），`SubType=4`（File Path），`Length = sizeof(NODE_HEADER) + FilePathLen * sizeof(CHAR16)`
4. 拼接：`[原路径（无结束节点）] + [文件路径节点] + [新结束节点]`
5. 通过 `AllocatePool(2, ...)` 分配（PoolType=2 = 引导服务数据），返回完整 Device Path

**`GetDevicePathSize()` 遍历逻辑：** 从首字节开始，通过 `Length = (Length[1] << 8) | Length[0]` 解码每个节点的大端长度，累加直到遇到 Type=0x7F、SubType=0xFF 的路径结束节点。若 `NodeSize < 4`（异常短节点），遍历立即终止以防止无限循环。

### 3.8 日志子系统

日志写入路径为 `\EFI\BOOT\unlock.log`（位于 ESP 分区），实现流程：

```
LogToFile(→Message)  [CHAR16 输入]
    ↓ BS->HandleProtocol(→gEfiLoadedImageProtocolGuid) → LoadedImage
    ↓ BS->HandleProtocol(→gEfiSimpleFileSystemProtocolGuid) → Volume
    ↓ Volume->OpenVolume(Volume, &RootDir)
    ↓ RootDir->Open(RootDir, &LogFile, "\\EFI\\BOOT\\unlock.log",
                    EFIFILEMODEREAD | EFIFILEMODEWRITE | EFIFILEMODECREATE, 0)
    ↓ LogFile->SetPosition(0xFFFFFFFFFFFFFFFF)  // 移到文件末尾
    ↓ CHAR16 → ASCII 转换（仅取低字节，丢弃高字节）
    ↓ 追加 CRLF（\r\n）
    ↓ LogFile->Write()
    ↓ LogFile->Close()  →  RootDir->Close()
```

**编码方案：** UEFI 字符串采用 UTF-16LE（`CHAR16`），日志输出则截取低字节转换为 7 位 ASCII 兼容格式。对于 ASCII 范围内的可打印字符（U+0000–U+007F），此转换无损保留信息。

**辅助格式化函数：**
- `StatusToHex(Status, Buffer)`: 将 `EFI_STATUS`（64 位）格式化为 `"0x"` + 16 个大写十六进制位。
- `LogStatusToFile()`: 前缀 + StatusToHex → LogToFile。
- `LogHexToFile()`: 前缀 + 逐字节 hex dump（空格分隔）→ LogToFile。


## 4 编译与部署

### 4.1 编译流水线

编译脚本 `build.bat`（60 行）定义了完整的从源码到 EFI 映像的自动化流水线：

```
补丁代码生成:
  python tools\generate_patches.py
    ↓ 扫描 patches/*.txt → 生成 src\GeneratedPatches.h

MOF 编译:
  mofcomp.exe -B:build\SsdtUnlockDB.bmf src\acpi\SsdtUnlockDB.mof
    ↓ 生成 SsdtUnlockDB.bmf（二进制 MOF）

BMF → ASL 转换 (PowerShell):
  ReadAllBytes('SsdtUnlockDB.bmf') → 十六进制数组
    ↓ 生成 BmfData.asl (Name(WQBA, Buffer(){...}))

ASL 编译 (iASL):
  iasl.exe -p SsdtUnlockDB -tc SsdtUnlockDB.asl
    ↓ 生成 SsdtUnlockDB.aml + SsdtUnlockDB.hex
    ↓ SsdtUnlockDB.hex 被 C 代码通过 #include 嵌入为 ssdtunlockdb_aml_code[]

C 编译 (MSVC cl.exe):
  cl.exe /GS- /W4 /O2 /utf-8 /TC /c
    main.c UefiHelpers.c Logging.c AcpiPatch.c
    ↓ 生成 .obj 文件

链接 (MSVC link.exe):
  link.exe /subsystem:efi_application /entry:efi_main
           /nodefaultlib /dll
    ↓ 生成 BOOTX64.efi
```

**表 6 — 编译器与链接器参数**

| 工具 | 关键参数 | 说明 |
|---|---|---|
| cl.exe | `/GS-` | 禁用缓冲区安全检查（UEFI 环境中无 CRT） |
| cl.exe | `/W4` | 最高警告级别 |
| cl.exe | `/O2` | 速度优化 |
| cl.exe | `/utf-8` | UTF-8 源文件编码 |
| cl.exe | `/TC` | 强制 C 编译模式 |
| link.exe | `/subsystem:efi_application` | UEFI 应用程序子系统 |
| link.exe | `/entry:efi_main` | 入口函数符号 |
| link.exe | `/nodefaultlib` | 不链接任何 CRT/默认库 |
| link.exe | `/dll` | 生成 PE DLL 格式（UEFI 要求） |

### 4.2 部署流程

部署通过 `singtools/debug_db.bat`（交互式菜单脚本，约 190 行）实现：

1. WMI 类检测：`Get-WmiObject -Namespace root\wmi -Class CustomDbUnlock`
2. DBFS 状态读取/写入
3. ESP 日志读取（`mountvol S: /S` → `type S:\EFI\BOOT\unlock.log`）
4. NVIDIA 功率查询（`nvidia-smi -q -d POWER`）
5. BOOTX64.efi 部署：`copy /Y BOOTX64.efi S:\EFI\BOOT\`
6. UEFI 引导项注册：通过 `bcdedit /copy {bootmgr}` 克隆引导管理器，设置 `device partition=S:`、`path \EFI\BOOT\BOOTX64.efi`、超时 3 秒


## 5 分析与讨论

### 5.1 方案优劣势分析

#### 优势

1. **极小依赖面**：不依赖 EDK2、gnu-efi 或任何第三方 UEFI 库。所有类型、协议和 GUID 手动声明，编译产物仅包含 MSVC CRT 的内置 `memcpy` 实现（通过 `#pragma function(memcpy)` 抑制编译器内建替换），使 EFI 映像体积约 73KB。

2. **完全透明性**：对 Windows 引导管理器不可见——引导加载器在注入完毕后立即链式加载 `bootmgfw.efi`，Windows 内核解析 ACPI 表时看到的已是重建后的 XSDT，无需修改任何引导配置数据库（BCD）项的操作系统路径字段。

3. **数据驱动架构**：补丁规格以纯文本文件（`patches/*.txt`）定义，通过 Python 代码生成器自动转换为 C 编译时数据。新增补丁仅需创建文本文件后重新运行构建脚本，无需修改 C 源码。补丁数据的变更（如调整目标功率值）仅涉及编辑十六进制串，无需重新逆向。

4. **跨表覆盖完整**：通过 FACP→DSDT 间接定位机制，不仅覆盖 XSDT 中直接引用的 SSDT4，还覆盖了 FACP 间接引用的 DSDT。这确保了对位于不同表族中的热管理代码路径（如 FNQS 方法中的 DBFS 条件分支）的完整覆盖。

5. **写时复制安全**：不修改 OEM 固件原始 ACPI 表的任何字节，仅重定向 XSDT/FACP 指针到影子页。如果移除该引导加载器，系统自动恢复到 OEM 默认功率限制。

6. **运行时可控**：通过 WMI 接口暴露的 `SetDbfs(0/1)` 方法允许在操作系统运行时动态开关 Dynamic Boost，无需重启。`_INI` 默认禁用策略确保安全启动。

7. **校验和完整性**：严格遵循 ACPI 规范的多阶段校验和重算流程——新 XSDT/FACP 校验和、各影子表校验和、RSDP 两级校验和，全部同步更新。

#### 局限与改进空间

1. **NumCandidates 硬编码错误**：`NumCandidates = 4` 而数组只有 2 个元素，应修正为 `sizeof(Candidates) / sizeof(Candidates[0])` 或直接使用 2。

2. **SearchAndReplace 的边界条件**：`for (i = 0; i <= Size - SearchSize; i++)` 在 `Size < SearchSize` 时会发生无符号整数下溢。虽然已有 `if (Size < SearchSize) return 0` 的守卫，但该守卫在函数入口处，循环条件在守卫之后——当 `Size == 0` 且 `SearchSize == 8` 时 `Size - SearchSize` 溢出为大正数，导致守卫正确阻止了执行。此逻辑正确但不够直观，建议显式采用 `Size >= SearchSize ? (Size - SearchSize) : 0` 风格。

3. **无 Secure Boot 处理**：引导加载器不检查 UEFI Secure Boot 状态。在启用 Secure Boot 的系统上，必须通过自定义密钥签名 `BOOTX64.efi`，或先将 Secure Boot 置于 Setup Mode / 禁用状态。

4. **NameString 索引的平台依赖性**：ATPP/ATP2/TPPA 补丁中的名称表索引（`0x0B`）硬编码于搜索字节序列中。该索引值依赖于 OEM 编译器对 SSDT ASL 代码的具体 NameString 分配，在其他固件版本或不同型号上可能需要重新逆向确定。CSPL/FPPT 补丁的 4 字节模式（索引 `0x0A`）同样存在此约束。

5. **补丁只读分发**：当前补丁系统设计为引导时一次性执行，不支持操作系统运行时动态调整补丁参数（WMI 接口仅控制 DBFS 标志，不涉及功率值修改）。

### 5.2 与现有方法的对比

| 维度 | 本方案 | Clover/OpenCore | acpi_override | 固件重刷 |
|---|---|---|---|---|
| 依赖库 | 无（纯 C 手写类型） | EDK2/OC 框架 | 依赖内核参数 | 需要硬件工具 |
| 配置文件 | 纯文本补丁规格（`patches/*.txt`） | config.plist 等 | grub/cmdline | 无 |
| 修改粒度 | 字节级二进制补丁（3–8 字节） | DSL 级覆盖 | 表级替换 | 任意（有风险） |
| 补丁扩展性 | 新增文件即可，无需改 C 源码 | 需编辑 plist + DSL | 需替换表文件 | 需完整重编译 |
| 运行时控制 | WMI 热开关 | 需重启 | 需重启 | 需重刷 |
| 安全风险 | 低（写时复制） | 低 | 低 | 高（硬砖风险） |
| 可逆性 | 删除 EFI 文件即恢复 | 切换引导器 | 移除内核参数 | 困难 |
| 映像大小 | ~73 KB | ~数 MB | N/A | N/A |

### 5.3 补丁效果参数量化

注：ATPP/ATP2/TPPA 寄存器采用 ×0.5 倍率（寄存器值 × 0.5 = 实际瓦数）；CSPL/FPPT 寄存器采用 ×1.0 倍率。

| 参数 | 原始寄存器值 | 修补寄存器值 | 实际功率变化 | 增益 |
|---|---|---|---|---|
| ATPP (Total Processor Power) | `0x0168` (reg=360) | `0x01E0` (reg=480) | 180W → 240W | +60W (+33.3%) |
| ATP2 (Adaptive Thermal Power 2) | `0x0168` (reg=360) | `0x01E0` (reg=480) | 180W → 240W | +60W (+33.3%) |
| TPPA (Total Platform Power Allow.) | `0x0168` (reg=360) | `0x01E0` (reg=480) | 180W → 240W | +60W (+33.3%) |
| CSPL — Ryzen 7 | `0x2D` (=45) | `0x3C` (=60) | 45W → 60W | +15W (+33.3%) |
| CSPL — Ryzen 9 | `0x37` (=55) | `0x3C` (=60) | 55W → 60W | +5W (+9.1%) |
| FPPT — Ryzen 7 | `0x41` (=65) | `0x50` (=80) | 65W → 80W | +15W (+23.1%) |
| FPPT — Ryzen 9 | `0x4B` (=75) | `0x50` (=80) | 75W → 80W | +5W (+6.7%) |
| cpu_clamp_unlock (DBFS 钳位) | `LEqual(DBFS, 1)` | `LEqual(DBFS, 0xFF)` | 限制分支永假 | 逻辑级解除 |
| cpu_limit_65w (CPU 限制) | `0xAFC8` (=45,000) | `0xFFFF` (=65,535) | 65W → 无限制 | 上限解除 |
| 厂商默认 ATPP（非 Ryzen 7） | `0x01B8` (reg=440) | — | 实际 220W（参考） | — |

注：`0x01E0`（实际 240W）并非任意上限值——它比厂商在未触发 Ryzen 7 + DGPU 条件时使用的默认 ATPP 值 `0x01B8`（实际 220W）还高出 20W，确保在满足所有前置条件时显卡均能达到其硬件 Dynamic Boost 最大能力。

### 5.4 安全性分析

1. **内存安全**：所有 ACPI 表拷贝通过 `UefiMemcpy` 完成，长度从表头 `Length` 字段读取（受信任的固件数据），不存在用户可控的长度参数。`IsValidAcpiPointer()` 的地址范围检查防止了通过 XSDT 损坏条目进行任意地址读写。

2. **日志安全**：日志路径硬编码为 `\EFI\BOOT\unlock.log`，文件名不可控。日志内容（ASCII 转换）最多 250 字符 + CRLF，`AsciiBuffer` 为栈上 256 字节数组，无缓冲区溢出风险。

3. **无网络暴露**：引导加载器无任何网络功能，仅在本地 UEFI 环境运行，完成后将控制权移交给 Windows Boot Manager。日志仅写 ESP 本地文件。WMI 接口的调用受操作系统权限模型保护（需管理员权限 + `Authentication PacketPrivacy`）。


## 6 结论

本文设计并实现了一个面向蛟龙 16 PRO 的专用 UEFI 引导加载器，在完全不依赖第三方 SDK 的前提下，通过重建 XSDT 注入自定义 WMI 设备 SSDT，并采用数据驱动的模块化补丁引擎——以纯文本规格文件定义补丁数据、Python 代码生成器自动生成 C 编译时数据结构、运行时间接遍历 FACP 指针修补 DSDT——对 OEM 固件中的 7 个功率参数（ATPP/ATP2/TPPA/CSPL×2/FPPT×2）和 2 个逻辑条件（DBFS 钳位 / 65W 限制）实施了精确的字节级修补。该方案成功将平台总功率限制从实际 180W 解锁至实际 240W（增益 +60W，+33.3%），同时将 CSPL 从 45W 提升至 60W、FPPT 从 65W 提升至 80W，并通过 `CustomDbUnlock` WMI 类暴露了运行时 Dynamic Boost 控制接口。

本方案的工程价值在于：
- 证明了在无 EDK2 等重型框架的情况下，仅通过手动声明 UEFI ABI 和 ACPI 结构体即可构建功能完备的引导级 ACPI 修改器；
- 提出了一种模块化、可扩展的补丁架构：补丁规格文件（`patches/*.txt`）→ 代码生成器（`generate_patches.py`）→ 数据驱动运行时（`gPatches[]` + `SearchAndReplace()`），将补丁逻辑与控制流完全解耦，实现了"新增补丁零代码修改"的工程目标；
- 验证了 FACP→DSDT 三级指针重定向在实际固件逆向场景中的可行性与必要性——仅修补 SSDT 无法覆盖位于 DSDT 中的热管理分支（FNQS 方法的 DBFS 条件）；
- 展示了"引导层拦截 + 链式加载"架构在 ACPI 表透明注入与多目标补丁场景下的工程简洁性与可靠性。

后续工作方向包括：通过静态分析 OEM SSDT/DSDT 自动提取 AML 操作码模式、实现补丁参数的运行时动态调整（如通过 WMI 接口修改功率上限值）、以及提供免 Secure Boot 签名的兼容方案。

## 参考文献（主要源码引用）

1. UEFI Specification, Version 2.10. UEFI Forum, 2024.
2. Advanced Configuration and Power Interface (ACPI) Specification, Version 6.5. UEFI Forum, 2022.
3. ACPI Component Architecture (ACPICA) — iASL Compiler/Disassembler. Intel Corporation.
4. Windows Management Instrumentation (WMI) ACPI — Microsoft Docs, "ACPI and WMI Interaction."
5. AMD Platform Security Processor (PSP) & SMU Power Management, AMD Developer Guides.
